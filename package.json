{
  "name": "heavy-props-renamer",
  "version": "1.0.0",
  "description": "Figma plugin to search for component property names",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.98.0",
    "typescript": "^5.3.0"
  }
}
